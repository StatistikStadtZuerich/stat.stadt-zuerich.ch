{
  "name": "stat.stadt-zuerich.ch",
  "version": "0.11.4",
  "description": "",
  "main": "index.js",
  "dependencies": {
    "trifid": "^2.1.0"
  },
  "devDependencies": {
    "bluebird": "^3.5.1",
    "node-fetch": "^1.7.3",
    "shelljs": "^0.7.8",
    "sparql-http-client": "^1.0.2"
  },
  "license": "UNLICENSED",
  "scripts": {
    "start": "trifid --config config.json",
    "start-local": "trifid --config config.local.json",
    "start-localstore": "trifid --config config.localstore.json",
    "update-api": "node ./build/update-api",
    "clean-api": "find -E . -type f -regex '.*/api/[A-Z]+.*' -delete"
  }
}
